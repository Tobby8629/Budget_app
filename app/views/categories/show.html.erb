<div class="inn">
<%= render partial:"shared/sign", locals:{model: categories_path, total:"$#{@category.exchanges.sum(:amount)}"} %>
  <%@exchanges.each do |e|%>
  <div class="each">
    <p><%=e.name%></p>
    <p>$<%=e.amount%></p>
  </div>
  <%end%>
<div class='add'>
  <%= link_to "Add Transaction", new_category_exchange_path(@category.id,cat_id: @category.id) %>
</div>
</div>